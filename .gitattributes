# Git LFS — large binary files that aren't model artifacts (those go
# through DVC instead, see todo.md section 4 / log.md for why: DVC's
# MinIO/S3 remote is the right fit for versioned model .pkl files, LFS
# covers everything else large and binary — reports, datasets, media).
*.pdf filter=lfs diff=lfs merge=lfs -text
*.pptx filter=lfs diff=lfs merge=lfs -text
*.parquet filter=lfs diff=lfs merge=lfs -text
*.h5 filter=lfs diff=lfs merge=lfs -text
*.onnx filter=lfs diff=lfs merge=lfs -text
*.pt filter=lfs diff=lfs merge=lfs -text
*.ckpt filter=lfs diff=lfs merge=lfs -text
