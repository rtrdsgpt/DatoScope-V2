# Minimal dependency set for the ETL pipeline (etl/, airflow/) — used to
# build the Airflow image, kept separate from requirements.txt so the
# Airflow container doesn't need Streamlit/Plotly/etc.
pandas>=2.0.0
numpy>=1.26.0
scikit-learn>=1.4.0
scipy>=1.12.0
boto3>=1.34.0
sqlalchemy>=1.4.36,<2.0
psycopg2-binary>=2.9.9
great-expectations>=1.0.0
kagglehub>=0.3.0
python-dotenv>=1.0.0
pyarrow>=15.0.0
